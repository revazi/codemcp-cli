coverage:
  precision: 2
  range: 80..100
  status:
    project:
      default:
        target: 80%
        threshold: 1%
    patch:
      default:
        target: 80%

comment:
  layout: reach, diff, flags, files
  behavior: default
  require_changes: true

github_checks:
  annotations: true
